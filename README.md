# FaceSwap API (Light Version)

This is a lightweight FastAPI backend for Render deployment.
Currently includes placeholder face swap logic.

## Run Locally
pip install -r requirements.txt
uvicorn main:app --reload

## Deploy on Render
Uses render.yaml for configuration.
